apiKey.id: myApiKeyId
apiKey.secret: myApkiKeySecret

different.apiKey.id: myDifferentApiKeyId
different.apiKey.secret: myDifferentApkiKeySecret

empty.apiKey.id:
empty.apiKey.secret:

stormpath:
  apiKey:
    id: myNestedApiKeyId
    secret: myNestedApkiKeySecret

different.stormpath:
  different.apiKey:
    different.id: myDifferentNestedApiKeyId
    different.secret: myDifferentNestedApkiKeySecret